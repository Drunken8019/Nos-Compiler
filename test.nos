extern:long GetStdHandle(int handle);
extern:char WriteConsoleA(long handle, char* msg, int msgSize, int reserved);

def:int main()
{
	let:char h = 104;
	let:char e = 101;
	let:char l = 108;
	let:char l1 = 108;
	let:char o = 111;
	
	let:char space = 32;
	
	let:char w = 119;
	let:char o1 = 111;
	let:char r = 114;
	let:char l2 = 108;
	let:char d = 100;

	let:int counter = 0;
	while(counter < 100)
	{
		print(&h, 11);
		counter += 1;
	}
	return 0;
}

def:char print(char* msg, int msgSize)
{
	let:long std_out = GetStdHandle(-11);
	WriteConsoleA(std_out, msg, msgSize, 0);
	return 0;
}